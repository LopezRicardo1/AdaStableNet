% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wald-network.R
\name{AdaStableNet_WaldNetwork}
\alias{AdaStableNet_WaldNetwork}
\title{Wald-Sparsified Directed Network from an AdaStableNet Fit}
\usage{
AdaStableNet_WaldNetwork(
  fit,
  Y,
  tt,
  branch = c("stable", "wald", "unbounded", "two_stage"),
  method = "BH",
  alpha = 0.05,
  diagonal = c("keep", "test", "zero"),
  nsteps = 20L,
  variance_ridge = 1e-06,
  stability_horizon = 1,
  stability_n_grid = 101L
)
}
\arguments{
\item{fit}{An \code{adastablenet_fit} or \code{adastablenet_stage} object.}

\item{Y}{Observed data in state-by-time or time-by-state orientation.}

\item{tt}{Observation times.}

\item{branch}{Branch used when \code{fit} is a complete fit. The two-stage option
uses the functional-data initializer and the first observed state as its
plug-in initial condition.}

\item{method}{Multiplicity method applied to off-diagonal p values and passed
to \code{\link[stats:p.adjust]{stats::p.adjust()}}.}

\item{alpha}{Edge-selection significance level.}

\item{diagonal}{Treatment of diagonal coefficients in \code{A_sparse}: retain all
(\code{"keep"}), test them as a separate multiplicity family (\code{"test"}), or
set them to zero (\code{"zero"}). This choice never changes the edge network.}

\item{nsteps}{Integration intervals for \code{\link[=TheoVar]{TheoVar()}}.}

\item{variance_ridge}{Fisher-information ridge multiplier.}

\item{stability_horizon}{Nonnegative horizon for transient-growth checks
before and after sparsification.}

\item{stability_n_grid}{Number of times in each transient-growth check.}
}
\value{
An object of class \code{adastablenet_wald_network} containing the full
coefficient estimate, standard errors, Wald scores, raw and adjusted p
values, edge table, binary adjacency matrix, weighted network,
sparsified dynamic matrix, and pre/post-sparsification stability
diagnostics.
}
\description{
Converts coefficientwise Wald tests into a directed interaction network for
the linear ODE \verb{X'(t) = A X(t)}. Entry \code{A[i, j]} represents the edge
\code{j -> i}. Multiple-testing adjustment is applied only to the \code{p * (p - 1)}
off-diagonal edges. Diagonal entries describe self-dynamics and are handled
separately rather than being counted as network edges.
}
\details{
The returned \code{adjacency} is binary, \code{A_network} contains the selected signed
edge weights with a zero diagonal, and \code{A_sparse} is a dynamic coefficient
matrix. By default, \code{A_sparse} retains every estimated diagonal coefficient
and only the selected off-diagonal coefficients. Because elementwise
thresholding does not preserve eigenvalues, stability diagnostics are
recomputed after sparsification.
}
\examples{
sim <- simulate_adastablenet(p = 3, n_time = 21, sigma = 0.03, seed = 4)
fit <- FitAdaStableNet(
  sim$Y, sim$time, nbasis = 8, twoSE = FALSE,
  num_iter = 30, wald_nsteps = 4, verbose = FALSE
)
net <- AdaStableNet_WaldNetwork(
  fit, sim$Y, sim$time, branch = "stable", nsteps = 4,
  stability_n_grid = 11
)
net$adjacency
net$A_sparse
}
\references{
Wu, L., Qiu, X., Yuan, Y.-X., and Wu, H. (2019).
Parameter estimation and variable selection for big systems of linear
ordinary differential equations: A matrix-based approach. \emph{Journal of the
American Statistical Association}, 114(526), 657-667.
}
