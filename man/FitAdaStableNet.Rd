% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FitAdaStableNet.R
\name{FitAdaStableNet}
\alias{FitAdaStableNet}
\title{Fit AdaStableNet: Adaptive ODE System Fitting Without FPC Basis}
\usage{
FitAdaStableNet(
  Y,
  tt,
  nbasis = 100,
  lambda_range = c(-20, 10),
  method = "two.stage",
  twoSE = TRUE,
  fit_ode2fd = TRUE,
  complex_pairs = TRUE,
  eigen_real_wald = TRUE,
  wald_critical = 2,
  eigen_bound = TRUE,
  lr = 0.001,
  num_iter = 5000,
  tol = 0.001,
  ridge.pen = 0.01,
  verbose = TRUE
)
}
\arguments{
\item{Y}{Matrix of observed data (timepoints × variables)}

\item{tt}{Vector of timepoints}

\item{nbasis}{Number of B-spline basis functions (default = 100)}

\item{lambda_range}{Exponent range for lambda grid (default = c(-20, 10))}

\item{method}{Method used in `.est()` (e.g., "two.stage", "pda")}

\item{twoSE}{Logical, if TRUE uses 2SE rule in GCV selection}

\item{fit_ode2fd}{If TRUE, fit model to smoothed data; else fit to raw data}

\item{complex_pairs}{If TRUE, augment real eigenvalues into complex pairs}

\item{eigen_real_wald}{If TRUE, apply Wald thresholding to real eigenvalues}

\item{wald_critical}{Wald threshold cutoff (default = 2)}

\item{eigen_bound}{If TRUE, cap real eigenvalues to ±1}

\item{lr}{Learning rate for AdaEigen_ODE}

\item{num_iter}{Number of optimization iterations}

\item{tol}{Tolerance for convergence}

\item{ridge.pen}{Ridge penalty for matrix inversion}

\item{verbose}{Logical, if TRUE print progress messages}
}
\value{
A list with components:
  \item{Ode2Stage}{Two-stage estimation output (including Ahat, Yhat_fd)}
  \item{AdaEigenStableNet}{Adaptive eigenvalue refinement output}
}
\description{
This function performs a two-stage estimation of the dynamic matrix \( A \)
followed by adaptive eigenvalue refinement using `AdaEigen_ODE`, without relying on
FPCA basis representations. The model is fit directly to the (smoothed) data.
}
