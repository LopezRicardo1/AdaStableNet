% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{FitAdaStableNet}
\alias{FitAdaStableNet}
\title{Fit AdaStableNet to a Multivariate Trajectory}
\usage{
FitAdaStableNet(
  Y,
  tt,
  nbasis = 25,
  lambda_range = c(-16, 4),
  method = "two.stage",
  twoSE = TRUE,
  fit_ode2fd = TRUE,
  complex_pairs = NULL,
  eigen_real_wald = TRUE,
  wald_critical = 2,
  eigen_bound = TRUE,
  stability_margin = 0,
  backend = c("base", "torch", "auto"),
  optimizer = "BFGS",
  num_iter = 1000L,
  tol = 1e-08,
  ridge.pen = 0.001,
  n_starts = 1L,
  start_jitter = 0.05,
  seed = NULL,
  wald_nsteps = 20L,
  variance_ridge = 1e-06,
  lr = NULL,
  torch_device = c("auto", "cpu", "cuda"),
  torch_refine = TRUE,
  torch_refine_iter = 20L,
  torch_patience = 5L,
  verbose = TRUE
)
}
\arguments{
\item{Y}{Numeric matrix with time points in rows and states in columns.}

\item{tt}{Strictly increasing observation times.}

\item{nbasis}{Number of B-spline basis functions.}

\item{lambda_range}{Range for the natural logarithm of the smoothing penalty.}

\item{method}{Gradient-matching initializer.}

\item{twoSE}{Use the two-standard-error smoothing rule.}

\item{fit_ode2fd}{Fit modal models to smoothed data when \code{TRUE}, otherwise raw
data.}

\item{complex_pairs}{Deprecated compatibility argument. Modal structure is
inferred from the real initial system matrix.}

\item{eigen_real_wald}{Apply an approximate Wald screen to modal real parts.}

\item{wald_critical}{Absolute normal critical value.}

\item{eigen_bound}{Fit a stability-constrained branch.}

\item{stability_margin}{Upper bound for active modal real parts.}

\item{backend}{Optimization backend: base R, Torch, or automatic selection.}

\item{optimizer}{Optimization method passed to \code{\link[stats:optim]{stats::optim()}}.}

\item{num_iter}{Maximum optimizer iterations per start.}

\item{tol}{Relative optimization tolerance.}

\item{ridge.pen}{Nonnegative scale-adjusted ridge multiplier.}

\item{n_starts}{Number of optimization starts.}

\item{start_jitter}{Standard deviation of parameter jitter after the first
start.}

\item{seed}{Optional seed for optimization starts.}

\item{wald_nsteps}{Integration intervals for approximate Wald variances.}

\item{variance_ridge}{Ridge multiplier for Fisher-information inversion.}

\item{lr}{Learning rate for the Torch Adam optimizer; ignored by base R.
\code{NULL} uses \code{0.01}.}

\item{torch_device}{Torch device: automatic CUDA selection, CPU, or CUDA.}

\item{torch_refine}{Refine the Adam solution with Torch L-BFGS.}

\item{torch_refine_iter}{Maximum L-BFGS refinement iterations.}

\item{torch_patience}{Consecutive small relative-loss changes required for
Adam convergence.}

\item{verbose}{Print concise progress messages.}
}
\value{
An object of class \code{adastablenet_fit}.
}
\description{
This is the main user interface. It smooths the observed states, constructs a
two-stage gradient-matching initializer, and fits profiled unconstrained,
Wald-screened, and stability-constrained modal models.
}
\examples{
sim <- simulate_adastablenet(p = 3, n_time = 31, sigma = 0.03, seed = 1)
fit <- FitAdaStableNet(
  sim$Y, sim$time, nbasis = 12, twoSE = FALSE,
  num_iter = 100, eigen_real_wald = FALSE, verbose = FALSE
)
fit
coef(fit)
}
